# End_Project
เพิ่มกิตกด git add .
เช็ค git status 
อัพขึ้นกด git push 
แจ้ง git commit -m" --- "
ดึงข้อมูล git pull origin main --rebase
git push


เรียงลำดับ
git add .
git commit -m "บันทึกก่อน pull"
git pull origin main --rebase
git push